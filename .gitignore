# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Env / secrets
.env

# Local dev compose overrides (use docker-compose.override.yml.example as a template)
docker-compose.override.yml

# Claude local dev helpers
.claude/

# Node / frontend
node_modules/
frontend/dist/
*.log

# Storage (uploaded resumes in local dev)
backend/storage/

# OS / editor
.DS_Store
.idea/
.vscode/

# Runtime / build artifacts (must never be tracked — they block git pull on servers)
backend/celerybeat-schedule
celerybeat-schedule*
*.tsbuildinfo
